name: Build VCamera APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      SDK_ROOT: ${{ github.workspace }}/android-sdk

    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      # Get latest cmdline-tools into a clean SDK dir
      - name: Install cmdline-tools (latest)
        run: |
          set -eux
          mkdir -p "$SDK_ROOT/cmdline-tools"
          curl -L -o /tmp/cmdline-tools.zip https://dl.google.com/android/repository/commandlinetools-linux-12266719_latest.zip
          unzip -q /tmp/cmdline-tools.zip -d "$SDK_ROOT/cmdline-tools"
          mv "$SDK_ROOT/cmdline-tools/cmdline-tools" "$SDK_ROOT/cmdline-tools/latest"
          echo "ANDROID_SDK_ROOT=$SDK_ROOT" >> $GITHUB_ENV
          echo "ANDROID_HOME=$SDK_ROOT" >> $GITHUB_ENV
          echo "$SDK_ROOT/cmdline-tools/latest/bin" >> $GITHUB_PATH

      # ✅ Accept licenses non-interactively; ignore SIGPIPE from `yes`
      - name: Accept SDK licenses
        run: |
          set -eux
          mkdir -p ~/.android && touch ~/.android/repositories.cfg
          yes | sdkmanager --sdk_root="$SDK_ROOT" --licenses >/dev/null || true

      # Install exact platform + build-tools
      - name: Install required platforms/build-tools
        run: |
          set -eux
          sdkmanager --sdk_root="$SDK_ROOT" "platform-tools" "platforms;android-34" "build-tools;34.0.0"

      # Use hosted Gradle (no wrapper in repo)
      - name: Setup Gradle (no cache)
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 8.10
          cache-disabled: true

      - name: Build debug APK (verbose)
        env:
          ANDROID_SDK_ROOT: ${{ env.SDK_ROOT }}
          ANDROID_HOME: ${{ env.SDK_ROOT }}
        run: gradle assembleDebug --stacktrace --info

      - name: Upload APKs
        uses: actions/upload-artifact@v4
        with:
          name: vcamera-apks
          path: "**/build/outputs/apk/**/*.apk"
